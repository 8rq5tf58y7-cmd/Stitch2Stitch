# Core dependencies
opencv-python>=4.5.0
opencv-contrib-python>=4.5.0  # For SIFT, MAGSAC++, USAC++
numpy>=1.20.0
scipy>=1.7.0
Pillow>=8.0.0

# Machine Learning (required for SuperPoint, deep learning matchers)
torch>=1.9.0
torchvision>=0.10.0
scikit-image>=0.18.0
scikit-learn>=1.0.0

# GUI
PySide6>=6.0.0
matplotlib>=3.4.0

# Performance
numba>=0.53.0
tqdm>=4.60.0

# Utilities
pyyaml>=5.4.0
tifffile>=2021.4.0  # For large TIFF support
psutil>=5.8.0  # For memory management

# Optional: HLOC for hierarchical localization
# pip install hloc  # Requires separate install

# Optional GPU acceleration
# Uncomment if you have CUDA installed
# torch[cuda]>=1.9.0

# Build tools (for creating standalone executables)
# pyinstaller>=5.0.0  # Uncomment to build standalone executables

